# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'KmmIosApplication' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for KmmIosApplication
  pod 'URLImage'
  pod 'shared', :path => ENV['KMM_SHARED_SAMPLE_FRAMEWORK_DIR']
end
